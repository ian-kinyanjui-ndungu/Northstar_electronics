PORT=3000
JWT_SECRET=northstar_jwt_secret_change_in_production
SESSION_SECRET=northstar_session_secret_change_in_production

# TiDB connection (MySQL-compatible)
# Update these with your TiDB Cloud or local TiDB credentials
DB_HOST=localhost
DB_PORT=4000
DB_NAME=northstar
DB_USER=root
DB_PASSWORD=
DB_SSL=false

# Set to true when connecting to TiDB Cloud (requires SSL)
# DB_SSL=true
